# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

알다마의 주 사용자는 여러 강의와 학교 활동을 동시에 관리하는 대학생이다. 사용자는 이동 중이거나 수업 사이의 짧은 시간에 휴대전화로 공지, 과제, 시험, 신청 및 행사 일정을 확인하고 처리한다.

## Product Purpose

흩어진 학교 공지와 학사 일정을 한곳에 모아 마감 상태를 빠르게 이해하고 놓치지 않도록 돕는다. 사용자는 공지 텍스트나 이미지를 분석해 일정을 추가하고, 직접 일정을 만들며, 목록과 캘린더에서 확인·수정·완료·삭제할 수 있다.

성공은 사용자가 모바일에서 중요한 일정과 다음 행동을 즉시 파악하고, 주요 작업을 2~3번 이내의 조작으로 끝내는 것이다.

## Positioning

알다마는 대학생의 공지 수집과 마감 관리에 집중한다. 일반 일정 앱과 달리 학교 공지 텍스트·이미지 분석과 LearningX/Canvas 일정 연동을 통해 일정 입력 부담을 줄이고, 중요도와 마감 상태를 학생의 작업 흐름에 맞게 보여준다.

## Operating Context

- 모바일 브라우저와 설치형 PWA에서 주로 사용하며 데스크톱 브라우저도 지원한다.
- 사용자는 학교 공지, 카카오톡 캡처, 이메일 내용, 직접 입력을 통해 일정을 추가한다.
- LearningX/Canvas 연결을 통해 외부 일정을 동기화하고 필요하면 연결을 해제한다.
- 알림 구독을 통해 예정된 일정 알림을 받을 수 있다.
- 공유된 공지는 `/share`에서 분석 결과를 확인하고 선택적으로 저장한다.

## Capabilities and Constraints

- Next.js App Router, React, TypeScript를 사용한다.
- Supabase Auth와 Database가 로그인, 세션 및 일정 데이터를 관리한다.
- Gemini가 공지 텍스트와 이미지 분석을 담당한다.
- LearningX/Canvas 연동, Netlify 배포 및 예약 함수를 유지한다.
- 회원가입, 로그인, 로그아웃, 인증 리디렉션과 쿠키 계약을 유지한다.
- 일정 생성, 수정, 완료, 되돌리기, 삭제, 목록 및 캘린더 기능을 유지한다.
- 기존 URL, search parameter, Server Action form 필드 이름을 유지한다.
- UI 작업만으로 Supabase 테이블, RLS, 마이그레이션 또는 Canvas 동기화 규칙을 바꾸지 않는다.
- 가능한 한 서버 컴포넌트를 유지하고 불필요하게 `use client` 범위를 확대하지 않는다.
- 새로운 대형 UI 라이브러리와 다크 모드는 별도 승인 없이는 추가하지 않는다.

## Brand Commitments

- 제품 이름은 `알다마`다.
- 차분한 남색과 청회색의 `콰이어트 월렛` 정체성을 유지한다.
- 펭귄은 단순 장식보다 빈 화면, 오류, 완료, 마감 임박 같은 상태 안내에 사용한다.
- 사용자 문구는 개발 용어 대신 대학생이 바로 이해할 수 있는 짧고 친근한 한국어를 사용한다.
- 선택된 디자인 방향은 `콰이어트 월렛`이다.

## Evidence on Hand

- 기존 펭귄 이미지: `public/mascot/`
- PWA 아이콘과 manifest: `public/icons/`, `public/manifest.json`
- 현재 디자인 토큰과 공통 UI 스타일: `src/app/globals.css`
- 디자인 계약: `DESIGN.md`
- 주요 제품 화면: `src/app/`, 특히 `src/app/dashboard/`

실제 사용자 인터뷰, 정량 사용 데이터, 추천사 및 공개 성과 지표는 현재 작업 범위에서 확인되지 않았다. 향후 화면 문구나 발표자료에서 이를 임의로 만들지 않는다.

## Product Principles

1. 마감과 다음 행동이 장식보다 먼저 보여야 한다.
2. 모바일 한 손 사용과 짧은 작업 시간을 우선한다.
3. 자동 분석과 연동은 입력 부담을 줄이되 사용자가 결과를 확인하고 수정할 수 있어야 한다.
4. 오류와 비활성 상태를 숨기지 않고 사용자가 취할 수 있는 다음 행동을 안내한다.
5. 기존 데이터, 인증 및 동기화 계약을 보존하면서 UI를 개선한다.

## Accessibility & Inclusion

- 최소 약 44px의 터치 영역과 명확한 키보드 포커스를 제공한다.
- 색상만으로 마감, 성공, 실패 또는 완료 상태를 구분하지 않는다.
- 입력창에는 접근 가능한 label을 제공하고 로딩·오류·성공 메시지를 보조기기가 인식할 수 있게 한다.
- 360px, 390px, 768px, 1024px 이상에서 안정적으로 동작해야 한다.
- 긴 과목명, 긴 공지 제목, 많은 일정에서도 정보와 행동에 접근할 수 있어야 한다.
